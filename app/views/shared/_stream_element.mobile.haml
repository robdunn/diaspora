-#   Copyright (c) 2010, Diaspora Inc.  This file is
-#   licensed under the Affero General Public License version 3 or later.  See
-#   the COPYRIGHT file.

.stream_element{:data=>{:guid=>post.id}}
  .right
    %span.time= how_long_ago(post)

  = person_image_link(person, :size => :thumb_small)

  .content
    .from
      =person_link(person)

    = render 'status_messages/status_message', :post => post, :photos => photos, :photo_size => :thumb_small

    .info
      = link_to "#{comments.length} #{t('comments.new_comment.comment').downcase} →", status_message_path(post), :class => 'comment'

